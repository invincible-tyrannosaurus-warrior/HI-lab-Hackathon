import React from "react";
import ReactDOM from "react-dom/client";
import StudentAgentDashboard from "../StudentAgentDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StudentAgentDashboard />
  </React.StrictMode>
);
